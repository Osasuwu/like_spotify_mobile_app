"""samples — example alternative-flavor configurations for contributors.

Populated as Phase 1 lands more extension points (Storage, Pre/PostActions).
A sample is a small, runnable end-to-end wiring that demonstrates a
non-default combination of extensions (e.g. Last.fm provider + cron trigger +
Google Sheets storage). Empty in #21 — the default tray flavor lives in
`like_spotify.hosts.tray` and is the reference example.
"""
