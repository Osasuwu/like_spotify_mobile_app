"""hosts — runtime shells that wire a Trigger + MusicProvider + feedback.

Phase 1 (#21) ships the default tray host. Issue #27 introduces a host split
(OneShotCliTrigger / headless CLI host) once the seam is justified by a
second host.
"""
